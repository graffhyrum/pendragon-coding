[build]
  command = "bun run build"
  publish = "dist"
  ignore = "exit 0"

# Disable auto-deploys from branch pushes
# Production deploys only happen via GitHub Actions on version tags
[[context.production]]
  ignore = "exit 0"

[[context.branch-deploy]]
  ignore = "exit 0"