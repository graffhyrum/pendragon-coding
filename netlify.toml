[build]
command = "bun run build"
publish = "dist"

# Git-connected builds run when Netlify detects eligible commits.
# Versioned releases still go through GitHub Actions on v* tags (see .github/workflows/deploy.yml).
