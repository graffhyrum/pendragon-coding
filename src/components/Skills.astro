---
import skills from '../content/skills';
import Skill from './Skill.astro';
---

<section >
	<h2 id="top-skills">Top Skills</h2>
	<ul role="list" class="skills-list">
		{skills.map((s, index) => (
			<Skill s={s} index={index} />
		))}
	</ul>
</section>
