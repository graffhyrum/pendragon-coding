---
import type { ContentTemplate } from '../content/types/ContentTemplate';

interface Props {
	containerContent: ContentTemplate;
}

const { containerContent } = Astro.props;
---
{containerContent.sections.map((section)=> (
<section>
    <h2>{section.title}</h2>
    <p class="text-xl">{section.subtitle}</p>
    <ul class="flex flex-wrap" role="list">
      {section.content.map((thisSection) => (
              <li class="outline-solid outline-2 outline-green-700 w-full max-w-lg m-4 p-2 rounded-lg">
                  <h3 class="text-3xl underline">{thisSection.title}</h3>
                  <ul>
                    {thisSection.link.map((link) => (
                            <li>
                                <a class="text-xl" target="_blank" href={link.href}>{link.title ?? "Link"}</a>
                            </li>
                    ))}
                  </ul>
                  <p class="text-lg">{thisSection.description}</p>
              </li>
      ))}
    </ul>
</section>
  ))}
