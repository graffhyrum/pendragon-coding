---
import Card from './Card.astro';

const { frontmatter, Content } = Astro.props;
---
<Card variant="long-form">
    <article id={frontmatter.title.replace(/ /g, '-')}>
        <Content />
    </article>
</Card>
