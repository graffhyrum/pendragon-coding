---
import Card from './Card.astro';

const { entry } = Astro.props;
const { Content } = await entry.render();
---
<Card maxWidth="max-w-max">
    <article id={entry.data.title.replace(/ /g, '-')}>
        <Content />
    </article>
</Card>
