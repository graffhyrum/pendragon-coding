---
import SidebarContentFragment from '../components/layout/SidebarContentFragment.astro';
import type { SidebarSections } from '../types/sidebarSections';

interface Props {
	sections: SidebarSections;
	hxHistory?: boolean;
}

const { sections, hxHistory } = Astro.props;
---

<SidebarContentFragment sections={sections} hxHistory={hxHistory}>
	<slot />
</SidebarContentFragment>
