---
'pendragon-coding': patch
---

Wire blog CollectionPageLayout to shared CardList stack layout (one post per row). Content pages keep CardList grid with auto-fill columns; remove viewport aspect-ratio rules that forced single-column grids on non-blog pages.
